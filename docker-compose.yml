services:
  portfolio:
    image: nginx:alpine
    container_name: meisi-digital-growth-portfolio
    ports:
      - "8088:80"
    volumes:
      - ./build:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
